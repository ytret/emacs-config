;; Emacs UI configuration.

(setq default-frame-alist `((left . 23) (top . 23)
			    (width . 80) (height . 44)
			    (font . "Hack 11")))

(menu-bar-mode -1)
(tool-bar-mode -1)
(scroll-bar-mode -1)
